package com.myapp.oops;

public class TestAbstraction {
	
	public static void display(Shape shape) {
		 
		System.out.println("Area of  "+shape.toString() +" is "+ shape.calculateArea());
	}

}

// what are the inclusions of a class
// variable | data members | fields (static | instance)

//methods | data members  (static | instance)